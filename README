# Install .tmux

```sh
cd
git clone git@github.com:gpakosz/.tmux.git
ln -s -f .tmux/.tmux.conf
ln -s -f dot_files/.tmux.conf.local .tmux.conf.local
```

Install patched font https://github.com/powerline/fonts/tree/master/SourceCodePro
